# Go + Linux — Deep labs (after lessons 1–12)

D1. strace a live Go server (accept/read/write/epoll under load)
D2. ListenConfig + SO_REUSEPORT (multi-process fan-in)
D3. Graceful drain — in-flight requests vs SIGTERM race
D4. GOMEMLIMIT vs cgroup / VmRSS
D5. TCP_INFO + socket options from Go
D6. sendfile / splice zero-copy
D7. netlink — list routes/addrs
D8. pprof mutex/block correlated with futex
